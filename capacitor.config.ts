import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.proyecto.notas',
  appName: 'Notes App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
